let Joi,CustomErrorHandler,User,bcrypt,JwtService;_cc8‍.x([["default",()=>_cc8‍.o]]);_cc8‍.w("joi",[["default",["Joi"],function(v){Joi=v}]]);_cc8‍.w("../../services/CustomErrorHandler",[["default",["CustomErrorHandler"],function(v){CustomErrorHandler=v}]]);_cc8‍.w("../../models",[["User",["User"],function(v){User=v}]]);_cc8‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_cc8‍.w("../../services/JwtService",[["default",["JwtService"],function(v){JwtService=v}]]);




const registerController = {
  async register(req, res, next) {
    const registerSchema = Joi.object({
      name: Joi.string().min(3).max(30).required(),
      email: Joi.string().email().required(),
      password: Joi.string()
        .pattern(new RegExp("^[a-zA-Z0-9]{3,30}$"))
        .required(),
      confirm_password: Joi.ref("password"),
    });
    _cc8‍.g.console.log(req.body);
    const { error } = registerSchema.validate(req.body);

    if (error) {
      return next(error);
    }

    // Check if user is in the database
    try {
      const exist = await User.exists({ email: req.body.email });
      if (exist) {
        return next(
          CustomErrorHandler.alreadyExist("This email is already taken")
        );
      }
      res.json({ message: "User registered successfully" });
    } catch (error) {
      return next(error);
    }

    // Hash Password
    const { name, email, password } = req.body;
    const hashedPassword = await bcrypt.hash(password, 10);
    //Prepare the model
    const user = new User({
      name,
      email,
      password: hashedPassword,
    });
    let access_token;
    try {
      const result = await user.save();
      _cc8‍.g.console.log(result);
      // Token
      access_token = JwtService.sign({
        _id: result._id,
        role: result.role,
      });
    } catch (error) {
      return next(error);
    }
    res.json({ access_token: access_token });
  },
};

_cc8‍.d(registerController);
